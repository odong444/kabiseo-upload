{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {},
  "deploy": {
    "startCommand": "gunicorn --worker-class eventlet -w 1 --timeout 120 --access-logfile - --bind 0.0.0.0:$PORT app:app"
  }
}
